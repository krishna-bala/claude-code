{
  "hooks": {
    "PostToolUse": [
      {
        "_comment": "Auto-format all supported files",
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/format.sh"
          }
        ]
      },
      {
        "_comment": "Run linters with auto-fix (optional - may be slower)",
        "matcher": "Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/lint.sh"
          }
        ]
      },
      {
        "_comment": "Git auto-stage modified files",
        "matcher": "Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path') && git add \"$FILE\" 2>/dev/null || true"
          }
        ]
      },
      {
        "_comment": "Log operations for debugging",
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p .claude && cat | jq -r '\"\\(.tool_name) on \\(.tool_input.file_path)\"' | xargs -I {} echo \"$(date): {}\" >> .claude/operations.log"
          }
        ]
      }
    ]
  },
  "_notes": [
    "Hooks receive JSON via stdin - use jq to parse",
    "These hooks are additive with global hooks",
    "Test with: echo '{\"tool_name\":\"Edit\",\"tool_input\":{\"file_path\":\"test.js\"}}' | your-command",
    "Keep hooks fast - they run synchronously",
    "Use absolute paths in commands"
  ]
}
